Django REST Framework Practice Project

This project is created to practice and learn the Django REST Framework (DRF) concepts by building simple APIs, experimenting with serializers, views, and models, and understanding how REST APIs work in Django.

📌 Purpose of the Project

To learn how Django REST Framework works.

To practice API development (CRUD operations).

To understand serializers, models, API views, and routers.

To experiment with authentication, permissions, and custom logic.

🚀 Features Practiced

Creating Django models

Using ModelSerializers

Using function-based and class-based API views

Performing CRUD operations

Using api_view, APIView, and ViewSets

Handling validations (custom & built-in)

Browsable API interface

Testing APIs using Postman / browser
